# Copy this file to .env and fill in real values. Do NOT commit .env.

# JWT signing key — use a long random string (at least 32 characters).
JWT_SIGNING_KEY=replace-with-a-long-random-string-at-least-32-chars

# SQL Server SA password (must meet SQL Server complexity requirements).
SA_PASSWORD=replace-with-a-strong-sql-server-password
